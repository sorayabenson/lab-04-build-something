//only .getById will send a text
//how do you send a text to a number that will be provided to the front end?

//sends `${ghost.name} says ${ghost.catchphrase}` with ghost.image

module.exports = class ContactService {
    static async create(contact) {
        
    }
}